sudo: required

services:
  - docker

before-install:
  - docker build -t voort66/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true voort66/react-test npm test

after_succes:
  - docker build -t voort66/multi-project ./client
